const SECONDS = 1000
const MINUTES = SECONDS * 60
const HOURS = MINUTES * 60
const DAYS = Hours * 24

const REFRESH_RATE = 5

module.exports {
    SECONDS,
    MINUTES,
    HOURS,
    DAYS,
    REFRESH_RATE
}